{{#if textType}}
    <input class="input {{class}}" name="{{name}}" type="text" placeholder="{{placeholder}}"/>
{{else}}
    <input class="input {{class}}" name="{{name}}" type="password" placeholder="{{placeholder}}"/>
{{/if}}